export const postReducer = (state = {}, action) => state;
export const especialityReducer = (state = {}, action) => state;
export const courseReducer = (state = {}, action) => state;
export const teacherReducer = (state = {}, action) => state;
export const classReducer = (state = {}, action) => state;
